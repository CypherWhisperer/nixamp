<?php
// info.php — full PHP build information.
// Confirms which extensions are loaded and what ini values are active.
// Delete or restrict access to this file in any non-local environment —
// phpinfo() exposes system internals that should never be public.
phpinfo();
